- 桌面端支持读取 Maoqiu Encryptor 的 `MAOLITE1\0` 轻量加密格式和 `MAOQIU1\0` 用户名/密码加密格式，macOS、Windows、Linux 打包产物同步生效。
- Android 和 iOS 端支持按文件头识别并打开 Maoqiu Encryptor `MAOLITE1\0` 轻量包，包括单文件和 tar bundle 内容。
- 媒体包打开流程支持任意后缀文件的 magic 识别，减少对 `.mqp` 后缀的依赖。
- 同步更新 Windows、Linux、Android、iOS 的版本号到 `0.1.16`。
